# 长安大学选课辅助筛选脚本安装说明

这个脚本是用来辅助筛选课程的，不会自动抢课，也不会保存你的账号密码。

## 第一步：安装 Tampermonkey

用 Edge 或 Chrome 浏览器打开扩展商店，搜索并安装：

Tampermonkey

安装完成后，浏览器右上角会出现 Tampermonkey 的图标。

## 第二步：安装脚本

打开这个脚本安装链接：

```text
https://github.com/Met3ORQAQ/chd-course-helper/raw/refs/heads/main/chd-course-helper.user.js
```

Tampermonkey 会自动弹出安装页面，点击“安装”就可以。

如果安装页面没有自动弹出，也可以用备用方法：

1. 点击浏览器右上角 Tampermonkey 图标。
2. 点击“添加新脚本”。
3. 删除编辑器里原本的内容。
4. 打开 `chd-course-helper.user.js`，复制里面的全部内容，然后粘贴进去。
5. 按 `Ctrl + S` 保存。

## 第三步：打开教务系统使用

1. 正常登录学校教务系统。
2. 进入选课页面。
3. 页面上方出现“选课辅助筛选”就说明安装成功。
4. 第一次使用建议点一次“后台读取课表”。
5. 然后按自己的需求勾选筛选条件。

## 常用功能

- `时间无冲突`：筛掉和当前课表冲突的课程。
- `早八友好课表`：筛掉第 1-2 节的课程。
- `有余量`：只看还有名额的课程。
- `无先修课程`：尽量筛掉有先修要求的课程。
- `考试方式`：可以选择考查或考试。
- `授课语言`：可以选择中文或双语。
- `教学楼位置`：可以选择 WX、WM、WH、WT 或其他。
- `只看匹配`：隐藏不符合条件的课程。

## 如果遇到问题

- 如果没有出现工具栏，先刷新选课页面。
- 如果课程冲突判断看起来不准，点一次“后台读取课表”。
- 如果浏览器拦截了新页面，允许弹出窗口后再点一次“后台读取课表”。
- 如果详情一直显示未知，可以点“读取匹配详情”，等待它慢慢读取。
- 如果不想继续读取详情，点“停止详情读取”。

## 使用提醒

- 脚本的筛选功能可以覆盖绝大多数常见课程，但无法保证准确覆盖选课列表中的所有课程。少数页面格式或课程安排比较特殊的课程可能不会被标绿，或者显示“未知”；“非标绿”不代表一定不能选，请结合课程安排、课程余量和自己的课表自行核对。
- 同时需要注意，当勾选“无先修课程”选项时，信息后台读取可能需要花费一些时间，请耐心等待。

## 安全提醒

- 不要把自己的教务系统账号密码发给别人。
- 这个脚本只在你自己的浏览器里运行。
- 这个脚本不会自动点击“选课”按钮。
- 同一台电脑换人登录后，建议重新点一次“后台读取课表”。
